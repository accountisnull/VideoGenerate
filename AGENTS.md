# Codex 项目约束

本文件适用于整个仓库。Codex 修改代码、配置、文档和执行 Git 操作时均应遵循。

## 团队统一开发规范

- 所有成员使用 Codex 开发时，统一遵循本文件及 [团队代码开发规范](docs/开发规范.md)。开发规范属于项目约束的一部分，必须在开始任务前阅读，并按改动范围执行；不能仅将其视为参考资料。
- 开始各模块任务时阅读 [成员开发任务入口](docs/成员开发任务入口.md) 和 [协作开工对齐](docs/协作开工对齐.md)，按已确认的目录、端口与交付边界执行，区分陈思锴确认的方案与其他成员尚未反馈的兼容性事项。
- 本文件集中列出 Codex 的执行要求，开发规范补充详细说明、示例和交接模板。修改共同条款时同步两处表述；发现不一致应说明并澄清，不自行挑选较宽松的规则。
- 本规范中的“用户”指当前委托 Codex 执行任务的成员。每位成员的推送授权只适用于其明确确认的本次变更，不替代其他成员的 PR 审批。
- 按任务需要阅读 [领域词汇](CONTEXT.md)、[技术框架](docs/技术框架.md)、[HTTP 接口协议](docs/HTTP接口协议.md) 和 [公共数据契约](docs/contracts/README.md)。确认文档的评审与实现状态，不把规划当成已实现功能。
- 一个任务明确输入、输出、验收条件、负责人和受影响模块；复杂任务在 `docs/` 中记录方案，小改动无需额外编写完整方案。未明确分配的前端、人物管理等工作应确认负责人，不自行推定归属。

## 沟通与修改

- 默认使用中文沟通，新增或修改的注释、说明文档使用中文；程序标识符、命令和标准名称保留原文。
- 只修改当前任务需要的内容，保留用户及其他成员尚未提交的改动。
- 开始前检查 `git status --short`、当前分支和远程仓库，确认工作范围。
- 未经用户要求，不创建长期按人员划分的分支，不引入 `develop` 分支。

## 模块边界与开闭原则

- 内容模块交付最终口播稿、发布标题和话题标签；配音模块交付最终配音及实测信息；数字人模块交付与配音对应的口型视频；集成模块负责流程编排、状态持久化、合成、发布规则和结果核实。
- 前三个模块通过本地 HTTP 向集成 Worker 提供能力，不互相串联，不直接执行抖音发布。供应商实现封装在对应模块或适配层，调用方不得读取其他模块内部数据库或直接依赖其业务实现。
- 新增和修改代码必须遵循开闭原则（Open–Closed Principle，OCP）：对扩展开放，对修改关闭。针对已明确的变化点，通过稳定接口和新增实现扩展能力，尽量保持已验证的核心逻辑与调用方不变。
- 新增文本模型、TTS 或数字人供应商时，优先新增符合现有契约的适配实现，在配置或组装入口选择；避免在路由、Worker 和业务模块中散布供应商判断分支。
- 可使用 Python Protocol、TypeScript interface、函数注入或组合，不强制继承，不为假设中的需求建立插件框架或多层抽象。修复缺陷、调整配置和演进业务规则仍可修改现有代码；涉及接口变化时说明影响、同步文档并验证兼容性。
- 公共接口、配置、依赖或数据库结构变更须说明受影响模块，由相关负责人参与评审。目录占位不构成功能需求，不夹带无关重构或扩展范围。

## 通用编码要求

- 文件使用 UTF-8，沿用所在文件的缩进和换行风格。命名体现业务含义和单位，遵循领域词汇，区分生产任务与模块作业、口型视频与成片、提交受理与发布成功。
- 函数围绕一个职责组织，使用明确参数、返回值和类型。复用已有公共规则；注释解释业务约束、设计原因及边界，不堆积注释掉的旧代码。
- 在边界校验外部输入，内部传递明确类型；不得静默吞掉异常或返回假成功。捕获可处理的具体异常，任务或请求边界的兜底处理须记录可追踪原因。
- 日志包含适用的任务编号、作业编号、请求编号、阶段和耗时，不记录密钥、令牌或完整敏感请求；返回客户端的错误不暴露堆栈或原始敏感响应。
- 新增依赖须说明用途，优先使用现有工具；通过依赖管理工具同步清单和锁文件，不手工修改锁文件。

## Python 后端要求

- 集成后端和内容模块使用 Python 3.11、uv；配音和数字人的独立模型环境由负责人提供可复现依赖文件及兼容说明，不强制并入集成环境。
- 模块、函数、变量使用 `snake_case`，类使用 `PascalCase`，常量使用 `UPPER_SNAKE_CASE`。新增公共函数及模块边界函数提供参数和返回类型标注，遵循现有 Ruff 配置。
- 请求、响应及配置复用已有 Pydantic 模型，公共传输模型使用 `backend/app/contracts/`。路由负责请求边界，业务逻辑、供应商调用和流程编排按职责组织。
- 跨模块流程由独立 Worker 编排，不依赖页面保持打开。前三个服务受理作业后异步执行并支持查询，不在提交请求中等待长时推理完成，不在异步路由内直接执行阻塞操作。
- HTTP 调用设置超时并按协议重试；SQL 使用参数化查询，幂等记录、作业建立和相关状态更新按原子性要求使用事务。数据库变更提供版本化迁移和已有数据的升级、恢复说明。
- 路径使用标准路径工具；调用 FFmpeg 等进程使用参数列表、检查退出码并设置适当超时，不将外部输入拼入 shell 命令。

## React / TypeScript 前端要求

- 使用函数组件和 Hooks，保留 TypeScript `strict`。组件、类型使用 `PascalCase`，函数和变量使用 `camelCase`，自定义 Hook 以 `use` 开头。
- 页面与共用组件分别放在 `frontend/src/pages/` 和 `frontend/src/components/`，按复用需要封装 API 访问、错误处理和轮询；优先复用 Ant Design 及已有样式。
- HTTP JSON 使用协议约定的 `snake_case`；前端字段转换在 API 边界显式完成。不得用随意的 `any`、非空断言或类型断言掩盖问题，静态类型不能替代外部数据校验。
- 表单提供输入校验，页面区分加载、空结果、失败和成功；控件有明确标签，列表使用稳定 key。防重复点击不能替代服务端幂等。
- 轮询在卸载、切换任务和进入终态时清理，防止过期响应覆盖当前状态。页面状态以服务端结果为准，不把请求中断当作任务取消，不把“已受理”显示为“已发布”。
- 模型及平台凭据只保留在服务端，不写入前端源码或 `VITE_*` 变量。

## 接口、状态与媒体交接

- 接口语义以适用版本的 HTTP 协议为依据，公共模型与生成的 JSON Schema 按契约文档使用；非 Python 实现补齐 Schema 未覆盖的业务校验，不手工编辑生成文件。
- 契约变化同步协议、模型、Schema、示例及测试。破坏兼容的变更按协议升级主版本；新增可选请求字段也要确认接收方兼容，并记录各模块版本与升级顺序。
- 区分可省略和可空字段，保留必填的 `null`。请求编号用于追踪，幂等键用于防重复；POST 超时后按协议以相同请求体、相同键核实或重试，不自动换键重做。
- 区分 HTTP 请求失败与业务失败，按协议维护不可变终态；状态转移、幂等和重启恢复须持久化，不能仅依赖数据模型校验。重启不得从头重复已完成的外部调用。
- 文件引用使用资产根目录下的相对路径，检查实际解析结果及链接不会越界。完整写入、验证可读、摘要、大小和实测媒体信息后才能报告产物成功。
- 口型视频与成片使用同一份最终配音，返工生成新作业和新产物，不覆盖仍被已完成任务引用的文件。媒体规格遵循协议，配置值不能代替实测值。
- 发布已提交、处理中、已发布和结果待核实分别记录；结果不明先查询核实，不自动重复发布。模拟服务或环境自检不代表真实模型生成、发布已经接通。

## 分支规则

- `main` 保存已验证、可运行的代码。禁止直接在 `main` 提交或推送业务、配置及文档变更。
- 每个任务使用一个短期分支，从最新的 `origin/main` 开始。
- 分支使用小写英文和连字符：新功能为 `feat/<功能名>`，修复为 `fix/<问题名>`，文档为 `docs/<主题>`，维护为 `chore/<任务名>`。
- 四人分工对应的分支示例：`feat/content-generation`、`feat/speech-service`、`feat/avatar-service`、`feat/generation-pipeline`。这些是任务分支，不是永久的人员分支。
- 本地只保留 `VideoGenerateDemo` 一个工作目录。未经用户明确允许，不创建额外工作树或项目副本。
- 当前已处于同一任务的分支时继续使用，不重复创建分支。切换任务前先确认现有修改归属；仅在不会覆盖或混入其他任务修改时切换分支。有未提交修改阻碍切换时，保留现场并与用户确认安排，不擅自清理工作区或创建额外工作树。
- 工作区干净且位于 `main` 时，按以下顺序开始任务；任何命令失败都应先处理原因，不继续执行后续步骤：

```powershell
git switch main
git pull --ff-only origin main
git switch -c feat/具体功能
```

- 工作区存在未提交改动时，先确定归属，不自动丢弃、覆盖或暂存其他人的工作；不得为清空工作区执行破坏性命令。

## 提交与推送

- 未经用户针对本次推送的明确许可，Codex 不得推送任何代码、配置或文档到任何远程分支。授权仅覆盖用户已确认的变更范围，不自动延续到后续新增或修改的内容；此前的推送授权、已有 PR 或要求修改文件均不能作为再次推送的许可。
- 用户仅要求“提交”时，只表示本地提交，不包含推送；要求“上传 Git”或“推送”时，仅在本次授权范围内执行，仍不得绕过 PR 直接推送 `main`。未经推送许可，保留本地修改或已获授权的本地提交，汇报状态并等待明确许可。
- 每次提交只完成一件明确的事，避免把无关重构、格式调整或其他人的修改一起提交。
- 提交前检查 `git diff`，按具体文件路径执行 `git add`，再检查 `git diff --cached` 和 `git diff --cached --check`。
- 提交说明采用 `<类型>: <中文说明>`，例如 `feat: 接入内容生成服务`、`fix: 修复任务恢复失败`、`docs: 补充分支管理规范`。类型使用 `feat`、`fix`、`docs`、`chore`、`refactor` 或 `test`。
- 首次推送使用 `git push -u origin <当前任务分支>`，后续只向该任务分支推送。
- 禁止强制推送、改写共享提交历史；不使用 `git reset --hard`、`git clean -fd` 等命令清理用户工作。
- 推送后核对远程分支与本地提交，汇报分支名、提交号、验证结果及 PR 状态。

## 同步主分支

- 同步前先提交当前任务已完成的修改，保持工作区干净。
- 在任务分支上执行以下命令，使用合并同步，避免改写已共享的历史：

```powershell
git fetch origin
git merge origin/main
```

- 遇到冲突应理解双方修改，解决后检查差异、运行相关验证，再提交；推送前仍须取得用户针对本次推送的明确许可。不得直接用整文件覆盖来掩盖冲突。

## PR 与合并

- 任务分支通过 GitHub PR 合并到 `main`。PR 写明具体问题、完成后的行为、验证结果及尚未完成的部分。
- 至少由另一名成员审批，相关检查通过后才可合并；推荐使用 **Squash and merge**，让一个任务对应主分支上的一个提交。
- Codex 自检或其他 AI 的评审不能代替另一名成员的审批。尚无 CI 时记录本地检查命令、结果及对应提交号供评审者核对；配置了远程必需检查后须等待其通过。
- 创建 PR 不等于获得合并授权。Codex 不得自行审批自己的 PR，也不得绕过审批或检查；只有用户明确要求合并且条件满足时才执行合并。
- 缺少 GitHub 工具、认证或权限时，保留已推送的任务分支，提供 PR 创建链接和待执行步骤，不声称 PR 已创建或已合并。
- 合并后删除远程任务分支，更新本地 `main`，并运行 `git fetch --prune` 清理失效的远程引用。
- 本地分支优先使用 `git branch -d <分支名>` 删除。Squash 合并导致删除被拒绝时，先确认对应 PR 已合并、没有未推送提交且工作区干净，再使用 `git branch -D <分支名>`；不按分支名称推断其可删除。
- 建议在 GitHub 为 `main` 开启分支保护：必须通过 PR、至少一人审批、相关检查通过、禁止强制推送。此文件是操作约束，不能代替 GitHub 的保护设置，不得声称仅添加文件就已开启远程保护。

## 验证要求

- 后端逻辑或配置读取发生变化时，在 `backend/` 下执行：

```powershell
.venv/Scripts/python.exe -m pytest -q
.venv/Scripts/python.exe -m ruff check app tests
```

- 前端代码或构建配置发生变化时，在项目根目录执行：

```powershell
npm.cmd --prefix frontend run build
```

- 公共模型变更时，在 `backend/` 下生成 Schema，再执行一致性检查及上述后端检查：

```powershell
.venv/Scripts/python.exe -m app.contracts.schema
.venv/Scripts/python.exe -m app.contracts.schema --check
```

- 配音、数字人等独立服务还须运行自身测试或自检，记录命令与结果；集成端检查不能替代独立服务验证。前端构建不能替代受影响页面的交互验证。
- 媒体工具或合成链路发生变化时，运行相关媒体自检。脚本变化时检查语法，并按影响范围验证行为。
- 媒体环境自检按安装说明完成配置并启动 API、Worker 后，在根目录执行 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/verify.ps1`；非默认端口添加与启动配置一致的 `-Port`。真实链路变化还需对应真实素材验证。
- 测试验证对外行为及适用的关键约束，包括重复提交、超时、失败恢复、路径越界和产物不匹配。普通自动化测试使用临时目录和替身服务，不依赖真实密钥、不产生收费请求或真实发布；真实联调单独记录环境、步骤与结果。
- 仅修改文档或注释时，检查内容、编码和 `git diff --check`，无需重复运行无关测试。
- 检查失败必须修复或明确报告；未执行的检查不得写成通过，不把环境自检通过描述为真实模型生成或发布已接通。

## 配置与敏感文件

- `.env` 仅保存在本机，不提交、不强制添加、不将其中的真实密钥写入输出、提交说明或 PR。
- 配置字段新增或变更时，同步更新 `.env.example` 和相关文档；模板使用中文注释，真实密钥留空。
- 通过统一入口读取并校验配置，不将模型 ID、供应商地址、端口、超时和资产路径散落在业务代码中，不写入个人机器的绝对安装路径。
- 集成后端和内容模块维护 `pyproject.toml` 与 `uv.lock`，前端维护 `package.json` 与 `package-lock.json`，按锁定版本安装。独立模型环境提交对应工具的可复现版本文件。
- 不同测试和开发实例隔离运行数据与端口；同一次联调的 API 与 Worker 按设计共享集成数据库，各模块按协议共享资产根目录。标准启停脚本只管理当前目录的一组 API 与 Worker，不设置其明确拒绝的进程级 `VIDEO_DATA_DIR` 环境变量。
- 不提交虚拟环境、依赖目录、构建产物、运行数据库、日志、模型权重和业务素材。
- 提交前检查暂存文件，确认没有本机路径配置、凭据或其他运行数据意外进入提交。

## 完成汇报与模块交接

- 完成任务后说明修改内容、受影响范围、实际验证结果和未完成事项，明确本地修改、本地提交、已推送、PR 已创建及已合并等状态，不混用。
- 模块交接提供启动方式、依赖及配置模板、服务地址与契约版本、就绪检查、请求响应示例、资产目录约定和已知限制；示例不含真实凭据。
- PR 和交接模板使用开发规范中的对应模板。简单修改可精简，但兼容性、迁移和未验证范围必须如实说明。
